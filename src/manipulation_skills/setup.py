from setuptools import setup
setup(name='manipulation_skills', version='0.1.0', packages=['manipulation_skills'],
      data_files=[('share/manipulation_skills', ['package.xml'])],
      install_requires=['setuptools'], zip_safe=True)
